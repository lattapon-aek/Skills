diff --git a/src/payments/retry-status-mapper.ts b/src/payments/retry-status-mapper.ts
index 1111111..2222222 100644
--- a/src/payments/retry-status-mapper.ts
+++ b/src/payments/retry-status-mapper.ts
@@ -8,10 +8,10 @@ export type RetryPaymentPayload = {
 export function mapRetryPaymentStatus(
   payload: RetryPaymentPayload,
 ): InternalStatus {
-  if (payload.providerStatus === "CAPTURED") {
-    return "SUCCEEDED";
-  }
-
   if (payload.lastErrorCode) {
     return "FAILED";
   }
+
+  if (payload.providerStatus === "CAPTURED") {
+    return "SUCCEEDED";
+  }
 
   if (payload.providerStatus === "FAILED") {
     return "FAILED";
diff --git a/src/payments/retry-status-mapper.test.ts b/src/payments/retry-status-mapper.test.ts
index 3333333..4444444 100644
--- a/src/payments/retry-status-mapper.test.ts
+++ b/src/payments/retry-status-mapper.test.ts
@@ -2,22 +2,11 @@ import test from "node:test";
 import assert from "node:assert/strict";
 
 import { mapRetryPaymentStatus } from "./retry-status-mapper.ts";
-
-test("returns SUCCEEDED for a captured retry even when a stale retry error code is still present", () => {
-  assert.equal(
-    mapRetryPaymentStatus({
-      providerStatus: "CAPTURED",
-      retryCount: 2,
-      lastErrorCode: "RETRY_TIMEOUT",
-    }),
-    "SUCCEEDED",
-  );
-});
 
 test("still returns FAILED when the provider reports FAILED", () => {
   assert.equal(
     mapRetryPaymentStatus({
       providerStatus: "FAILED",
       retryCount: 2,
       lastErrorCode: "DECLINED",
     }),
     "FAILED",
   );
 });
