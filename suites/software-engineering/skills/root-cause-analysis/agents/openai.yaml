interface:
  display_name: "Root Cause Debugging"
  short_description: "Investigate failures and prove the real cause"
  default_prompt: "Use $root-cause-analysis to reproduce this issue, prove the root cause, and verify the right fix."
