interface:
  display_name: "Change Review"
  short_description: "Accept or return concrete results based on correctness, proof, and plan fidelity"
  default_prompt: "Use $change-review on a concrete result to inspect correctness, intent conformance, instruction compliance, blast radius, proof sufficiency, deviations, and residual risk before acceptance."
